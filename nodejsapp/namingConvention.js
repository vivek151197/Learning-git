function add(a,b) {
return a + b;    //spacingBetweenOperators
}
//brackets convention : space for opening bracket and closing bracket in next line
console.log(add(1,2));

var person = {
firstName: "John",	//camelCasing
secondName: "Doe"
};
//semicolon at the end of object list
console.log(person.firstName + " " + person.secondName);
//line length < 80
